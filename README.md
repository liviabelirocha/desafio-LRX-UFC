# Desafio LRX UFC :rocket:

## Instruções :memo:

#### **Frontend**

1. Instale as dependências:

   `yarn` ou `npm install`

2. Inicie a aplicação:

   `yarn` ou `npm start`

#### **Backend**

1. Instale as dependências:

   `yarn` ou `npm install`

2. Inicie a aplicação:

   `yarn` ou `npm start`
